# 中国期货交易 - 项目知识库

**项目路径：** `D:\期货数据\铜期货监控`
**最后更新：** 2026-02-04
**触发关键词：** 中国期货交易

---

## 一、项目概述

### 项目目标
对中国期货市场22个品种进行策略回测和优化，开发实盘监控系统。

### 核心策略
- **策略名称：** EMA-MACD-RSI-STC 组合策略
- **信号类型：** 狙击信号 + 追涨信号
- **持仓管理：** 单一持仓（不允许同时多仓）
- **止盈方式：** STC高位回落、趋势反转、止损

### 交易品种（22个）
**上期所（SHFE）：** 沪铜、沪铝、沪锌、沪镍、沪锡、沪铅、螺纹钢、热卷
**郑商所（CZCE）：** PTA、甲醇、白糖、棉花、纯碱、玻璃
**大商所（DCE）：** 铁矿石、焦炭、焦煤、豆油、豆粕、棕榈油、PP、PVC

---

## 二、重大问题与修正历史

### 🔴 问题1：错误的合约规格（最严重）

**错误做法：**
- 所有品种统一使用 5吨/手
- 所有品种统一使用 15% 保证金

**实际情况：**
```python
FUTURES_SPEC = {
    # 上期所 (SHFE)
    '沪铜': {'contract_size': 5, 'margin_rate': 0.08},      # 5吨/手, 8%
    '沪铝': {'contract_size': 5, 'margin_rate': 0.08},      # 5吨/手, 8%
    '沪锌': {'contract_size': 5, 'margin_rate': 0.08},      # 5吨/手, 8%
    '沪镍': {'contract_size': 1, 'margin_rate': 0.12},      # 1吨/手, 12%
    '沪锡': {'contract_size': 1, 'margin_rate': 0.13},      # 1吨/手, 13%
    '沪铅': {'contract_size': 5, 'margin_rate': 0.08},      # 5吨/手, 8%
    '螺纹钢': {'contract_size': 10, 'margin_rate': 0.08},   # 10吨/手, 8%
    '热卷': {'contract_size': 10, 'margin_rate': 0.08},     # 10吨/手, 8%

    # 郑商所 (CZCE)
    'PTA': {'contract_size': 5, 'margin_rate': 0.07},       # 5吨/手, 7%
    '甲醇': {'contract_size': 50, 'margin_rate': 0.08},     # 50吨/手, 8%
    '白糖': {'contract_size': 10, 'margin_rate': 0.08},     # 10吨/手, 8%
    '棉花': {'contract_size': 5, 'margin_rate': 0.08},      # 5吨/手, 8%
    '纯碱': {'contract_size': 20, 'margin_rate': 0.08},     # 20吨/手, 8%
    '玻璃': {'contract_size': 20, 'margin_rate': 0.08},     # 20吨/手, 8%

    # 大商所 (DCE)
    '铁矿石': {'contract_size': 100, 'margin_rate': 0.08},  # 100吨/手, 8%
    '焦炭': {'contract_size': 100, 'margin_rate': 0.08},    # 100吨/手, 8%
    '焦煤': {'contract_size': 60, 'margin_rate': 0.08},     # 60吨/手, 8%
    '豆油': {'contract_size': 10, 'margin_rate': 0.08},     # 10吨/手, 8%
    '豆粕': {'contract_size': 10, 'margin_rate': 0.08},     # 10吨/手, 8%
    '棕榈油': {'contract_size': 10, 'margin_rate': 0.08},   # 10吨/手, 8%
    'PP': {'contract_size': 5, 'margin_rate': 0.08},        # 5吨/手, 8%
    'PVC': {'contract_size': 5, 'margin_rate': 0.08},       # 5吨/手, 8%
}
```

**影响：** 收益率虚高4-20倍
- 焦煤：+960.56%（错误）→ +180.74%（正确）-81.2%
- 沪镍：+810.10%（错误）→ +146.65%（正确）-81.9%
- 玻璃：+761.49%（错误）→ +118.20%（正确）-84.5%

---

### 🔴 问题2：多重持仓（严重错误）

**错误代码：**
```python
# 允许在持有仓位时继续开仓
if buy_signal and position is not None:
    # 继续开新仓！
    position = {...}
```

**正确代码：**
```python
# 只在没有持仓时才考虑开仓
if position is not None:
    # 检查平仓条件
    ...
    continue  # 跳过开仓逻辑

# 只有在空仓时才检查开仓信号
if buy_signal and position is None:
    position = {...}
```

**影响：** 隐性杠杆，实际风险被低估

---

### 🔴 问题3：沪锡保证金比例错误

**错误：** 使用12%保证金
**正确：** 沪锡最低保证金13%

**验证结果：**
- 12%保证金：+458.51%
- 13%保证金：+362.92%
- 差异：-20.8%

---

### 🟡 问题4：保守参数"无交易"Bug

**现象：** 使用保守参数（30%仓位，2%单笔风险）时显示"无交易"

**原因：**
```python
contracts = int((capital * 0.02) / potential_loss_per_contract)
# 沪锡案例：int(100000 * 0.02 / 5627) = int(0.35) = 0
```

**修复：**
```python
if allow_min_contracts and contracts < 1:
    if margin_per_contract <= capital:
        contracts = 1  # 至少开1手
```

---

## 三、信号生成逻辑（核心）

### 狙击信号（Sniper Signal）
```python
trend_up = current['ema_fast'] > current['ema_slow']
ratio_safe = (0 < current['ratio'] < RATIO_TRIGGER)
ratio_shrinking = current['ratio'] < prev['ratio']
turning_up = current['macd_dif'] > prev['macd_dif']
is_strong = current['rsi'] > RSI_FILTER

sniper_signal = trend_up and ratio_safe and ratio_shrinking and turning_up and is_strong
```

**特征：**
- 5个条件必须全部满足
- 在趋势确认、动能蓄积时提前介入
- 收益风险比最佳

### 追涨信号（Chase Signal）
```python
ema_cross = (prev['ema_fast'] <= prev['ema_slow']) and (current['ema_fast'] > current['ema_slow'])
chase_signal = ema_cross and is_strong
```

**特征：**
- EMA金叉 + 强势确认
- 趋势已经启动，追涨买入
- 交易机会更多

### 卖出信号
```python
# 1. STC止盈
stc_exit = (prev_stc > STC_SELL_ZONE) and (current_stc < prev_stc)

# 2. 趋势反转
trend_exit = current['ema_fast'] < current['ema_slow']

# 3. 止损
stop_loss_exit = current['low'] <= stop_loss_price
```

---

## 四、最终优化结果（TOP 10）

| 排名 | 品种 | 收益率 | 交易数 | 胜率 | 合约单位 | 保证金 | 最优参数 |
|------|------|--------|--------|------|---------|--------|---------|
| 🥇 | 沪锡 | +362.92% | 18笔 | 55.6% | 1吨 | 13% | EMA(3,10), RSI=35, RATIO=1.25, STC=75 |
| 🥈 | 焦煤 | +180.74% | 15笔 | 60.0% | 60吨 | 8% | EMA(12,10), RSI=35, RATIO=1.05, STC=75 |
| 🥉 | PTA | +167.47% | 13笔 | 84.6% | 5吨 | 7% | EMA(12,10), RSI=40, RATIO=1.25, STC=75 |
| 4 | 沪镍 | +146.65% | 5笔 | 60.0% | 1吨 | 12% | EMA(3,20), RSI=55, RATIO=1.10, STC=75 |
| 5 | 玻璃 | +118.20% | 14笔 | 57.1% | 20吨 | 8% | EMA(12,10), RSI=35, RATIO=1.10, STC=75 |
| 6 | 沪铝 | +98.12% | 18笔 | 55.6% | 5吨 | 8% | EMA(3,15), RSI=50, RATIO=1.05, STC=75 |
| 7 | 沪铜 | +70.90% | 12笔 | 75.0% | 5吨 | 8% | EMA(3,20), RSI=35, RATIO=1.05, STC=75 |
| 8 | 沪锌 | +63.88% | 11笔 | 54.5% | 5吨 | 8% | EMA(7,20), RSI=35, RATIO=1.25, STC=75 |
| 9 | 棕榈油 | +60.82% | 12笔 | 75.0% | 10吨 | 8% | EMA(12,10), RSI=45, RATIO=1.20, STC=75 |
| 10 | 纯碱 | +52.48% | 7笔 | 71.4% | 20吨 | 8% | EMA(12,20), RSI=35, RATIO=1.05, STC=75 |

---

## 五、信号质量分析（不含虚拟资金）

### 信号质量评分标准
- 胜率评分（40分）
- 盈亏比评分（30分）
- 最大连续亏损（15分）
- 平均盈利（15分）

### TOP 10 优质信号（按质量评分排序）

| 排名 | 品种 | 质量评分 | 胜率 | 盈亏比 | 平均盈利 | 交易数 | 特征 |
|------|------|---------|------|--------|---------|--------|------|
| 1 | **PTA** | 83.8分 | 84.6% | 11.08 | +1.42% | 13笔 | 高胜率型 |
| 2 | **沪镍** | 81.0分 | 60.0% | 19.35 | +6.36% | 5笔 | 高盈亏比型 |
| 3 | **棕榈油** | 80.0分 | 75.0% | 3.06 | +1.28% | 12笔 | 平衡型 |
| 4 | **纯碱** | 78.6分 | 71.4% | 5.26 | +1.64% | 7笔 | 高盈亏比型 |
| 5 | **PVC** | 78.3分 | 83.3% | 6.74 | +0.85% | 6笔 | 高胜率型 |
| 6 | **沪铜** | 77.0分 | 75.0% | 8.49 | +1.43% | 12笔 | 平衡型 |
| 7 | **豆粕** | 76.7分 | 66.7% | 3.59 | +1.28% | 6笔 | 平衡型 |
| 8 | **沪锡** | 76.2分 | 55.6% | 6.19 | +4.42% | 18笔 | 高盈亏比型 |
| 9 | **沪铅** | 73.3分 | 83.3% | 2.55 | +0.75% | 12笔 | 高胜率型 |
| 10 | **玻璃** | 71.9分 | 57.1% | 2.40 | +3.16% | 14笔 | 高盈利型 |

---

## 六、风险管理参数对比

### 激进参数（回测用）
```python
MAX_POSITION_RATIO = 0.8      # 80%仓位
MAX_SINGLE_LOSS_PCT = 0.15    # 单笔15%亏损
```
**结果：** 收益极高但风险极大，不适合实盘

### 保守参数（实盘推荐）
```python
MAX_POSITION_RATIO = 0.3      # 30%仓位
MAX_SINGLE_LOSS_PCT = 0.02    # 单笔2%亏损
allow_min_contracts = True    # 允许最少1手
```
**结果：**
- 沪锡：+121.78%（18笔，平均1.0手）
- PTA：+13.72%（13笔，平均4.0手）

### 极度保守参数（新手推荐）
```python
MAX_POSITION_RATIO = 0.2      # 20%仓位
MAX_SINGLE_LOSS_PCT = 0.01    # 单笔1%亏损
```

---

## 七、实盘监控配置

### 当前实盘参数（copper_monitor.py）
```python
EMA_FAST = 3
EMA_SLOW = 20
RSI_FILTER = 40       # ⚠️ 与回测35不同
RATIO_TRIGGER = 1.2   # ⚠️ 与回测1.05不同
STC_SELL_ZONE = 85    # ⚠️ 与回测75不同
STOP_LOSS_PCT = 0.015 # ⚠️ 与回测0.02不同
```

### 回测优化参数（reoptimize_realistic.py）
```python
EMA_FAST = 3          # ✓ 相同
EMA_SLOW = 20         # ✓ 相同
RSI_FILTER = 35       # 优化结果
RATIO_TRIGGER = 1.05  # 优化结果
STC_SELL_ZONE = 75    # 优化结果
STOP_LOSS_PCT = 0.02  # 优化结果
```

### 建议
**将实盘参数更新为优化后的值，以获得与回测一致的信号**

---

## 八、关键文件结构

```
D:\期货数据\铜期货监控\
├── futures_monitor.py                   # 🌟 多品种监控系统（TOP 10）- 推荐
├── 启动监控.bat                          # 多品种监控启动脚本
├── 多品种监控使用说明.md                  # 多品种监控使用说明
├── copper_monitor.py                    # 实盘监控系统（沪铜-单品种）
├── reoptimize_realistic.py              # 回测优化代码（22品种）
├── analyze_signal_quality.py            # 信号质量分析
├── backtest_risk_corrected.py           # 风险参数对比
├── verify_xi_real_margin.py             # 沪锡保证金验证
├── verify_pta_real_margin.py            # PTA保证金验证
├── china_futures_fetcher.py             # 数据获取模块
├── notifier.py                          # Telegram通知模块
├── SIGNAL_LOGIC_COMPARISON.md           # 信号逻辑对比报告
├── 中国期货交易知识库.md                 # 本文件
│
├── futures_data_4h/                     # 4小时K线数据
│   ├── 沪铜_4hour.csv
│   ├── 沪锡_4hour.csv
│   └── ...
│
├── optimization_results_realistic/      # 优化结果
│   ├── final_summary_corrected.csv      # 最终结果
│   ├── FINAL_REPORT_CORRECTED.md        # 最终报告
│   ├── progress.csv                     # 进度记录
│   └── SIGNAL_QUALITY_REPORT.csv        # 信号质量报告
│
└── logs/
    ├── multi_positions.json             # 多品种持仓状态（新增）
    ├── multi_signals.json               # 多品种交易日志（新增）
    ├── multi_tracking.csv               # 多品种运行记录（新增）
    ├── multi_monitor.log                # 多品种运行日志（新增）
    ├── signal_log.json                  # 单品种信号日志
    ├── position_status.json             # 单品种持仓状态
    ├── performance_tracking.csv         # 单品种表现追踪
    └── strategy_monitor.log             # 单品种监控日志
```

---

## 九、多品种监控系统（2026-02-04新增）

### 系统概述

**文件：** `futures_monitor.py`
**功能：** 同时监控TOP 10优质信号品种
**特点：**
- ✅ 每个品种使用独立的最优参数
- ✅ 独立的持仓状态管理
- ✅ 每4小时自动运行（0:00, 4:00, 8:00, 12:00, 16:00, 20:00）
- ✅ 统一的Telegram推送
- ✅ 不记录具体金额，只记录持仓状态

### 监控品种（TOP 10）

| 排名 | 品种 | 质量评分 | 胜率 | 盈亏比 | 最优参数 |
|------|------|---------|------|--------|---------|
| 1 | **PTA** | 83.8分 | 84.6% | 11.08 | EMA(12,10), RSI=40, RATIO=1.25, STC=75 |
| 2 | **沪镍** | 81.0分 | 60.0% | 19.35 | EMA(3,20), RSI=55, RATIO=1.10, STC=75 |
| 3 | **棕榈油** | 80.0分 | 75.0% | 3.06 | EMA(12,10), RSI=45, RATIO=1.20, STC=75 |
| 4 | **纯碱** | 78.6分 | 71.4% | 5.26 | EMA(12,20), RSI=35, RATIO=1.05, STC=75 |
| 5 | **PVC** | 78.3分 | 83.3% | 6.74 | EMA(3,25), RSI=55, RATIO=1.05, STC=75 |
| 6 | **沪铜** | 77.0分 | 75.0% | 8.49 | EMA(3,20), RSI=35, RATIO=1.05, STC=75 |
| 7 | **豆粕** | 76.7分 | 66.7% | 3.59 | EMA(12,20), RSI=35, RATIO=1.25, STC=75 |
| 8 | **沪锡** | 76.2分 | 55.6% | 6.19 | EMA(3,10), RSI=35, RATIO=1.25, STC=75 |
| 9 | **沪铅** | 73.3分 | 83.3% | 2.55 | EMA(12,10), RSI=40, RATIO=1.05, STC=75 |
| 10 | **玻璃** | 71.9分 | 57.1% | 2.40 | EMA(12,10), RSI=35, RATIO=1.10, STC=75 |

### 运行方式

#### 方法1：启动脚本（推荐）
双击 `启动监控.bat`，选择：
1. 单次运行 - 立即执行一次
2. 定时运行 - 每4小时自动运行

#### 方法2：命令行
```bash
# 单次运行
python futures_monitor.py

# 定时运行
python futures_monitor.py --scheduled
```

### 输出文件

| 文件 | 说明 |
|------|------|
| `logs/multi_positions.json` | 所有品种的持仓状态 |
| `logs/multi_signals.json` | 所有开仓/平仓交易记录 |
| `logs/multi_tracking.csv` | 每次运行的汇总记录 |
| `logs/multi_monitor.log` | 详细运行日志 |

### 持仓管理示例

```json
{
  "PTA": {
    "holding": false,
    "entry_price": null,
    "entry_datetime": null,
    "stop_loss": null,
    "signal_id": null
  },
  "沪镍": {
    "holding": true,
    "entry_price": 125000.0,
    "entry_datetime": "2026-02-04 08:00:00",
    "stop_loss": 122500.0,
    "signal_id": "2026-02-04 08:00:00_sniper"
  }
}
```

### 核心优势

1. **参数独立** - 每个品种使用最优参数，不用妥协
2. **状态独立** - 同时跟踪多个品种的持仓
3. **信号准确** - 与回测使用相同的信号逻辑
4. **自动化** - 定时运行，无需人工干预
5. **轻量化** - 不记录金额，只跟踪状态

### 使用建议

**测试阶段：**
```bash
# 使用单次运行，手动检查信号
python futures_monitor.py
```

**实盘阶段：**
```bash
# 使用定时运行，24小时监控
python futures_monitor.py --scheduled
```

**详细说明：** 查看 `多品种监控使用说明.md`

---

## 十、重要结论

### 1. 信号质量 > 虚拟收益
- 用户看重的是优化后参数的交易信号质量
- 收益率是虚拟的，受仓位管理影响巨大
- 信号质量（胜率、盈亏比）才是真实的

### 2. 保证金比例的重要性
- 12% → 13%：沪锡收益率下降20.8%
- 7% vs 8%：PTA收益率相同（风险限制是瓶颈）
- 必须使用真实的交易所保证金比例

### 3. 合约单位的决定性影响
- 沪锡（1吨）：收益率最高 +362.92%
- 铁矿石（100吨）：收益率较低 +23.59%
- 合约单位直接影响杠杆倍数

### 4. 参数一致性
- 实盘监控与回测优化应使用相同参数
- 当前实盘参数更保守（RSI=40 vs 35）
- 建议更新为优化参数以验证回测结果

### 5. 风险管理的必要性
- 激进参数（80%仓位，15%单笔）：收益高但风险极大
- 保守参数（30%仓位，2%单笔）：收益低但可承受
- 实盘必须使用保守参数

---

## 十、实盘建议

### 🔥 激进型投资者
**推荐品种：** 沪锡、焦煤
**理由：**
- 沪锡：+362.92%，盈亏比3.67
- 焦煤：+180.74%，胜率60%
**风险：** 波动大，连续亏损风险高

### ⚖️ 平衡型投资者
**推荐品种：** PTA、玻璃、沪铝
**理由：**
- PTA：+167.47%，胜率84.6%（最稳定）
- 玻璃：+118.20%，中等收益风险
- 沪铝：+98.12%，交易机会多
**风险：** 收益适中，回撤可控

### 🛡️ 保守型投资者
**推荐品种：** 沪铜、PP、沪铅
**理由：**
- 沪铜：+70.90%，胜率75%，重点关注品种
- PP：+40.36%，胜率100%
- 沪铅：+34.26%，胜率83.3%
**风险：** 交易机会少，但胜率高

### ⚠️ 风险提示
1. 回测收益不等于实盘收益
2. 需考虑滑点、手续费、市场冲击成本
3. 期货交易风险巨大，可能损失全部本金
4. 建议从小资金开始验证策略
5. 严格执行止损，不可抗单

---

## 十一、技术指标计算

### EMA（指数移动平均）
```python
df['ema_fast'] = df['close'].ewm(span=EMA_FAST, adjust=False).mean()
df['ema_slow'] = df['close'].ewm(span=EMA_SLOW, adjust=False).mean()
```

### MACD & Ratio
```python
exp1 = df['close'].ewm(span=12, adjust=False).mean()
exp2 = df['close'].ewm(span=26, adjust=False).mean()
df['macd_dif'] = exp1 - exp2
df['macd_dea'] = df['macd_dif'].ewm(span=9, adjust=False).mean()
df['ratio'] = df['macd_dif'] / df['macd_dea']  # 关键指标
```

### RSI（相对强弱指标）
```python
delta = df['close'].diff()
gain = (delta.where(delta > 0, 0)).ewm(alpha=1/14, adjust=False).mean()
loss = (-delta.where(delta < 0, 0)).ewm(alpha=1/14, adjust=False).mean()
rs = gain / loss
df['rsi'] = 100 - (100 / (1 + rs))
```

### STC（Schaff Trend Cycle）
```python
stc_macd = df['close'].ewm(span=23, adjust=False).mean() - df['close'].ewm(span=50, adjust=False).mean()
stoch_k = 100 * (stc_macd - min_macd) / (max_macd - min_macd)
stoch_d = stoch_k.rolling(window=3).mean()
stc_raw = 100 * (stoch_d - min_stoch_d) / (max_stoch_d - min_stoch_d)
df['stc'] = stc_raw.rolling(window=3).mean()
```

---

## 十二、数据来源

### K线数据
- **数据源：** AkShare API
- **周期：** 4小时
- **历史：** 300天
- **备用：** 本地CSV文件

### 实时监控
- **运行频率：** 每4小时
- **数据更新：** 自动获取最新数据
- **通知方式：** Telegram Bot

---

## 十三、用户重点关注事项

### ✅ 已确认正确
1. 信号生成逻辑（实盘监控 vs 回测代码）100%一致
2. 技术指标计算公式完全相同
3. 合约规格已使用真实数据
4. 保证金比例已核实（沪锡13%，PTA7%）
5. 单一持仓限制已实现

### ⚠️ 需要注意
1. 实盘参数与优化参数不一致
2. 虚拟收益率不等于实盘收益率
3. 必须使用保守的风险管理参数
4. 信号质量比收益率更重要

### 📋 建议行动
1. 更新实盘监控参数为优化后的值
2. 小资金测试1-2个月
3. 对比实盘信号与回测信号的一致性
4. 逐步放大资金规模
5. 持续记录和分析交易日志

---

**知识库版本：** v1.0
**最后更新：** 2026-02-04
**维护者：** Claude Code
**触发关键词：** 中国期货交易
