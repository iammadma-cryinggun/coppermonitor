# 待办事项（铜期货监控项目）

> 2026-09-04 整理验证后遗留事项

## 1. 监控系统恢复（当前停摆，2026-02-06 后无数据）

### 现状
- 监控数据自 2026-02-06 后无更新
- 部署目录下的监控脚本（`deploy/`）依赖 `china_futures_fetcher.py`

### 恢复步骤

**① 恢复 `china_futures_fetcher.py`（已从仓库删除）**

该文件已在上次"清理GitHub仓库"提交中删除，可从 git 历史找回：

```bash
cd "D:\期货数据\铜期货监控"
git show 0e0b842~1:china_futures_fetcher.py > china_futures_fetcher.py
# 或更早版本（包含更多历史逻辑）:
git show HEAD~2:china_futures_fetcher.py > china_futures_fetcher.py
```

恢复后确认 `Dockerfile` / `deploy/china_futures_fetcher.py` 引用关系一致。

**② 重跑 4h 数据获取**

```bash
cd "D:\期货数据\铜期货监控"
python 下载白银4h数据_v2.py      # 4h 数据补采
python run_monitor.bat           # 启动监控
```

**④ 验证**

- 检查监控日志是否恢复更新（应出现 2026-09-04 后的数据）

## 2. 回测/验证修复建议（源自《验证报告.md》）

- **CCI `全品种严格回测.py` 仓位规则未执行文档声称的 30% 上限**
  - 现状：`max_value = balance * 0.9 * self.leverage`（90%×2x 全仓复利 → +660%）
  - 建议：改为 `min(balance * 0.9 * leverage, balance * 0.30 * leverage)`（→ +84.3%）
  - 收益数字发布需标注仓位假设（全仓 +660% vs 30%仓位 +84%）
- **数据-合约错配**：LME 铜（USD/吨，25 吨/手）数据套用 SHFE 合约乘数 5
  - 当前 `CONTRACT_SIZE = 5` 是 SHFE 规格，套在 LME 数据上会低估每手名义价值、高估可开手数
  - 修复方向：LME 数据使用 25 吨/手，或用 SHFE 沪铜数据（`daily_backtest/CU_沪铜_日线.csv`）重跑
- **LME铜回测系统**：修复后 +67.72% / -5.18% DD / 125笔 / 60.0% 胜率（已更新 README）

## 3. 验证报告位置

- `验证/验证报告.md` — CCI 铜 +660% 严格样本外验证完整结果
- 核心结论：策略 edge 真实（样本外 +125.4%、参数高原、成本不敏感），但 +660% 被仓位规则夸大约 683%（30% 上限实为 +84.3%）
