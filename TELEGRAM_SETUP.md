# Telegram 配置指南（图文教程）

## 步骤1: 创建Telegram Bot

### 1.1 打开Telegram

在手机或电脑上打开Telegram应用

### 1.2 搜索 BotFather

1. 点击搜索图标 🔍
2. 搜索 `@BotFather`
3. 点击进入聊天

### 1.3 创建新Bot

1. 发送命令: `/newbot`
2. BotFather会回复: "Alright, a new bot. How are we going to call it? Please choose a name for your bot."

### 1.4 设置Bot名称

1. 输入bot的显示名称（如: `我的期货监控`）
2. 点击发送
3. BotFather会回复: "Good. Now let's choose a username for your bot. It must end in `bot`. Like this, for example: TetrisBot or tetris_bot."

### 1.5 设置Bot用户名

1. 输入bot的用户名（必须以`bot`结尾）
   - 示例: `my_copper_monitor_bot`
2. 点击发送

### 1.6 保存Token

BotFather会返回Token：
```
Done! Congratulations on your new bot. You will find it at t.me/my_copper_monitor_bot. You can now add a description, about section and profile picture for it, etc.

Use this token to access the HTTP API:
123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

**重要**: 复制并保存这个Token（格式：`数字:字母数字混合`）

---

## 步骤2: 获取Chat ID

### 2.1 找到你的Bot

1. 在Telegram搜索框输入你刚创建的bot用户名
2. 点击进入bot聊天

### 2.2 发送启动命令

1. 发送 `/start` 命令
2. Bot会回复欢迎消息

### 2.3 访问API获取Chat ID

1. 在浏览器地址栏输入以下URL（替换YOUR_BOT_TOKEN）：
   ```
   https://api.telegram.org/bot123456789:ABCdefGHIjklMNOpqrsTUVwxyz/getUpdates
   ```

2. 如果没有消息，返回：
   ```json
   {"ok":true,"result":[]}
   ```
   这表示没有新消息，等待几秒后刷新页面

3. 如果有消息，返回类似：
   ```json
   {
     "ok": true,
     "result": [
       {
         "update_id": 123456789,
         "message": {
           "message_id": 1,
           "from": {
             "id": 987654321,  <-- 这就是你的Chat ID
             "is_bot": false,
             "first_name": "Your Name",
             "username": "your_username"
           },
           "chat": {
             "id": 987654321,  <-- 这也是你的Chat ID
             "first_name": "Your Name",
             "type": "private"
           },
           "date": 1234567890,
           "text": "/start"
         }
       }
     ]
   }
   ```

4. 复制 `id` 字段的值（纯数字，如 `987654321`）

---

## 步骤3: 填写配置文件

### 3.1 打开配置文件

文件位置: `D:\期货数据\铜期货监控\config\telegram.json`

### 3.2 编辑配置

将复制的Token和Chat ID填入配置文件：

```json
{
  "token": "123456789:ABCdefGHIjklMNOpqrsTUVwxyz",
  "chat_id": "987654321"
}
```

**注意**:
- `token` 是完整字符串，包括冒号
- `chat_id` 是纯数字，不要加引号外的内容

### 3.3 保存文件

保存并关闭配置文件

---

## 步骤4: 测试通知

### 4.1 运行测试脚本

打开命令提示符（CMD）或PowerShell：

```bash
cd "D:\期货数据\铜期货监控"
python notifier.py
```

### 4.2 检查Telegram

如果配置成功，你会收到测试消息：
```
🧪 测试消息：沪铜监控系统已启动
```

### 4.3 如果没收到消息

**检查清单**:
1. ✅ Token是否正确（没有多余空格）
2. ✅ Chat ID是否正确（纯数字）
3. ✅ 是否向bot发送过 `/start` 命令
4. ✅ 配置文件是否保存为UTF-8编码
5. ✅ 网络连接是否正常

**调试方法**:
- 查看错误日志: `logs\strategy_monitor.log`
- 重新运行: `python notifier.py`

---

## 步骤5: 配置定时任务

### 5.1 创建Windows定时任务

1. 右键点击 `setup_task.bat`
2. 选择"以管理员身份运行"
3. 等待任务创建完成

### 5.2 验证任务

1. 按 `Win + R`
2. 输入 `taskschd.msc` 回车
3. 在任务列表中找到"沪铜监控"
4. 查看任务详情和下次运行时间

---

## 常见问题

### Q1: Token在哪里找到？

**A**: Token只在创建bot时显示一次，需要保存好。如果忘记了：
1. 打开 @BotFather
2. 发送 `/mybots`
3. 选择你的bot
4. 点击 "API Token"
5. 选择 "Revoke current token"（如果需要）
6. 或者Token会显示在页面中

### Q2: Chat ID可以在Telegram应用里看到吗？

**A**: 不能直接看到，必须通过API获取。最简单的方法：
1. 向你的bot发送任意消息
2. 访问 `getUpdates` API
3. 在返回的JSON中找到 `chat.id`

### Q3: 可以群组接收通知吗？

**A**: 可以！
1. 将bot添加到群组
2. 在群组中发送 `/start@your_bot_name`
3. 访问 `getUpdates` API
4. 找到群组的 `chat.id`（通常是负数，如 `-1001234567890`）
5. 将群组Chat ID填入配置文件

### Q4: 如何禁用Telegram通知？

**A**: 两种方法：
1. 删除或重命名 `config/telegram.json`
2. 或将配置文件内容留空：
   ```json
   {
     "token": "",
     "chat_id": ""
   }
   ```

### Q5: 通知延迟多久？

**A**: 通常<5秒。如果延迟超过10秒：
- 检查网络连接
- Telegram服务器可能繁忙
- 查看日志文件了解详细错误

---

## 安全建议

1. **保护Token**: Token等同于你的bot密码，不要分享给他人
2. **限制Bot权限**: 只授予bot必要的权限
3. **定期更换**: 可以定期更换Token（通过BotFather）
4. **监控日志**: 定期检查 `logs/` 目录，确保系统正常运行

---

## 配置完成！

一旦你收到测试消息，说明配置成功！

接下来：
1. 运行 `setup_task.bat` 创建定时任务
2. 系统将每4小时自动发送监控报告到你的Telegram

**需要帮助？**
- 查看日志: `logs\strategy_monitor.log`
- 查看文档: `README.md`
- 测试通知: `python notifier.py`
